
pub fn simulated_annealing<M, T>(
  solution: &mut T,
  num_iterations: u32,
)
where
  M: LocalMove<T>,
  T: Solution,
{
  // Determine temperature
  
  // Main loop
}

fn metropolis_rule() -> f64 {
    //...
    0.0
}
